# Spec: deniz-dotnet-general review follow-up (T items)

Date: 2026-08-05

Planning scratch (AGENTS.md, Documentation Hygiene): delete when the work merges.

## Goal

Close out the s12 review of `deniz-dotnet-general` (commit `5c36e57`): remove content the curator
will not use, repair every reference the curation itself broke, fix the one defective patch, set
ceremony triggers to the curator's ruling, and retire the two OpenCode emitter gaps the ruling
makes louder. No shortcuts: no shipped body may point at a skill that does not exist in the set,
no example may teach a dead API, no decision may contradict its own recorded rationale.

## Rulings (curator, this session)

| # | Ruling |
|---|---|
| T1 | Cut `grade-tests`, `exp-mock-usage-analysis`, `exp-test-maintainability`, `find-untested-sources`, `migrate-vstest-to-mtp`, `migrate-xunit-to-xunit-v3`, `mjml-email-templates`, `migrate-dotnet8-to-dotnet9`, and `migrate-dotnet9-to-dotnet10`. Record each as `exclude: true` with a reason; never delete a considered item from the manifest. The version-migration reference closures are bundled files, so no separate dependency cut follows from removing their parent skills. |
| T2 | `testcontainers-integration-tests`: complete the 3.x modernization the existing patch claims — remove the named pre-3.0 API spellings, fix the readonly-assignment defect in `MigrationTests`, and add a short module-packages section whose sample includes its required namespace. |
| T3 | Every artifact reference from a kept body to an excluded item is rewired or removed. The surviving homes claim only the practical work their corrected bodies perform: qualitative assertion-depth review in `test-anti-patterns`, targeted method/class/file CRAP analysis from existing coverage rows in `coverage-analysis`, and general-purpose Verify guidance in `snapshot-testing` with email-specific branches removed. Patches preserve each corpus's bare-reference convention; touching a body does not itself create a namespaced fact or `depends_on` (ADR-0008). |
| T4 | Retained repo/system-mutating ceremonies flip `both` → `manual`. A kept body that redirects to a manual ceremony preserves its bare name and says the user invokes it separately; the model never chains into it. `both` remains only on report-only items the curator also types (`slopwatch`, `analyzing-dotnet-performance`) and `dotnet-devcert-trust` (model reaches it on its exact error). |
| T5 | Manifest header: drop the false "none carries disable-model-invocation" claim and the "honour the upstream fire-design" rationale; state the real rule (honest descriptions → `auto` is safe; mutating ceremonies → `manual`). |
| T6 | ROADMAP: add the own TUnit test-writing skill as a deferred item with its own authoring session. Until it lands, test-writing redirects are removed, and the gap is recorded beside the `writing-mstest-tests` exclusion. |
| T7 | Emitter: a bundled `manual` conversion parks its full body as `BODY.md` beside its bundle and emits a stub command pointing at it; links to `SKILL.md` inside parked files are repointed at `BODY.md`. Retires the parked-reference gap and the wall-paste gap for bundled `manual` items; bundle-less `manual` remains inline. |
| T8 | No `expects` machinery now. Recorded in ROADMAP as a deferred design with an explicit trigger (load-bearing bare-name edges accumulating in untouched bodies). `grill-me` → `grilling` stays deliberately unguarded. |

## Non-goals, each with its revisit trigger

- **`expects` field** — build only if unguarded load-bearing edges accumulate (T8 note).
- **MSTest rows inside multi-framework reference tables** — stay; inert lookup data, useful in
  foreign repos.
- **Stub commands for `both` items** (e.g. `brainstorming`'s 150-line paste) — out of scope;
  revisit if the wall-paste cost annoys in practice. The Known Gap narrows to this class plus
  bundle-less `manual` commands.
- **Module-package rewrite of every testcontainers example** — a note section is added; full
  rewrite only if the skill's generic examples mislead in practice.
- **Full assertion-diversity metrics and the academic 19-smell catalog** — not absorbed. The
  pragmatic audit gains a bounded assertion-depth check and keeps its existing common-smell table;
  revisit only if metrics or academic citations become a real workflow.
- **Removing general rendered-HTML snapshot guidance** — not part of the email cut. Verify's generic
  HTML/output support remains; only email-authoring sections, examples, and dead handoffs leave.

## End state (verification targets)

- `npm run build` clean; `npm run validate` **0 errors**.
- Wave A introduces no warning identity beyond the standing pre-wave set. Wave B retires the
  `teach` and `writing-great-skills` dead-`SKILL.md` link warnings; the remaining standing findings
  keep their existing identities.
- Taken/excluded and invocation counts are derived from the final manifest, inventory, and ledger at
  close-out. Hand-written planning documents do not pin totals that later curation can stale.
- A fresh reference-audit sweep (docs/agents/reference-audit-playbook.md) over
  `deniz-dotnet-general` reports zero true excluded-artifact references in kept descriptions,
  bodies, and bundled markdown; false-positive candidates are listed separately with reasons.
- Wave A `docs/ledger.json` diff reviewed by item identity: the T1 cut list disappears; the retained
  ceremony list changes invocation; `run-tests`, `mtp-hot-reload`, `test-anti-patterns`,
  `test-gap-analysis`, `coverage-analysis`, `dotnet-webapi`, `test-analysis-extensions`,
  `snapshot-testing`, `detect-static-dependencies`, `dotnet-trace-collect`,
  `generate-testability-wrappers`, and `migrate-static-to-wrapper` gain `body: patch`; the named
  description deltas match the plan, and bare
  references do not create fact/dependency deltas merely because their bodies are patched; no
  unrelated entry changes.
- Wave B `docs/ledger.json` diff reviewed: every bundled `manual` item gains `BODY.md` in
  `opencode.parked`; no invocation, edge, description, or artifact-kind delta.
- The exact Wave B stub is exercised from project-local and global OpenCode mounts before the
  manual wall-paste and parked-link gaps are declared retired.

## Execution order

Wave A (curation, plan `2026-08-04-wave-a-general-curation.md`) → Wave B (emitter, plan
`2026-08-04-wave-b-opencode-stub-commands.md`) → then the pre-existing backlog (P1 split, P2/P3
sessions) untouched by this spec.
