interface:
  display_name: "To Tickets"
  short_description: "Break a plan, spec, or the current conversation into a set of..."
  default_prompt: "Use $deniz-process:to-tickets for this task."
policy:
  allow_implicit_invocation: false
