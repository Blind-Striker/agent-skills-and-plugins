interface:
  display_name: "Handoff"
  short_description: "Compact the current conversation into a handoff document for..."
  default_prompt: "Use $deniz-process:handoff for this task."
policy:
  allow_implicit_invocation: false
