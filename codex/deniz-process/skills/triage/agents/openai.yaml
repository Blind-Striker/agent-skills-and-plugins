interface:
  display_name: "Triage"
  short_description: "Move issues and external PRs through a state machine of triag..."
  default_prompt: "Use $deniz-process:triage for this task."
policy:
  allow_implicit_invocation: false
