interface:
  display_name: "Codex Handoff Ibis"
  short_description: "Explicit-only cross-skill handoff probe."
  default_prompt: "Use $codex-probe:codex-handoff-ibis and return the nested skill's marker."
policy:
  allow_implicit_invocation: false
